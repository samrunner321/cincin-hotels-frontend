// Re-export the layout from src/app
export { default, metadata } from '../src/app/layout';

// This layout is being deprecated in favor of the one in src/app
// The app will automatically use the src/app layout instead